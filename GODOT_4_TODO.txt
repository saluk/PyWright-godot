[X] Music and Sound can't be load() directly from filesystem. iterate through audiostream classes to load from file
[ ] Font issues
	[X] Dynamic ttf file not loading
	[X] make sure font size is corrected for
	[ ] label.get_font -> label.theme.get_font
	[X] switch from get()/set() for theme_override to get_theme_override and add_theme_override functions
	[X] label.autowrap -> autowrap_mode = TextServer.AUTOWRAP_WORD_SMART
[ ] Eliminate warnings
[ ] shader compile issues
[ ] can't import Node3D
[ ] Gui.gd "join" in "array"
[X] Image loading issues
	[X] create_from_img turned into static
	[X] Investigation menu (loading specific rects from graphics?)
	[X] cross exam buttons
[ ] Tweens (check)
	[ ] Check scrolls
	[ ] Scroll saving
	[ ] Check warp
	[X] Tween.new -> node.add_tween
	[X] Tween.start -> Tween.play
	[X] Tween.tween_completed -> Tween.finished
	[X] tween.interpolate_property -> tween.tween_property
	[X] tell -> get_total_elapsed_time
[ ] not compiling
[X] Reenable scriptdebugger
	[X] set_owner call when copying the node out of the scene needed to be moved
	[X] We used to use "bookmark" feature of TextEdit to keep track of current frame's line even if you scroll around:
		need to switch this to using the fancier godot 4 "gutters" concept
	[X] enable line numbers (filling them into the gutter)
	[X] highlight the current line number by changing its background color
	[ ] highlight lines that have been executed? 
	[X] click on any gutter to jump to the line
	[ ] Actually, we need to switch from TextEdit to CodeEdit, but that's a bigger change
[X] frames -> sprite_frames
[ ] figure out how to handle yield frames in WrightScriptStack.gd
	[X] make flash not use await - we can't return await during script execution
	[ ] look for other cases where we return an awaitable function
[ ] mouse dragging in examine
[ ] Node3D
[ ] Fixing themes for games menu ui and options tab ui
[X] SignalUtils removing some important signals that are needed for remove_child() to work
[X] Change game folder not working - needed to set file_mode of dialog to FileDialog.FILE_MODE_OPEN_DIR
[X] Load saved game not removing the case menu
[ ] Some print statements say "refcounted"
[ ] object_factory.txt test mode not working, some issues with ut functions
	[X] RefCounted testing script wasn't sticking around to run code after the await process_frame
	[X] visible_within isn't working
	[X] timing differences in object_factory.txt
	[ ] examine test has error - can't find examine menu
	[ ] allow speed mode to work with ut (adjust frame counters when running fast? use something other than frame counters?)
[ ] get_shader_param -> get_shader_parameter
[ ] set _debug true not making text print quick

[X] idle_frame -> process_frame
[X] material.gdshader -> material.shader
[X] animation.playing
[X] EVAL_EXPR can't return String() - do we need to make a converter? turns out you just use str()
[X] args.join(Commands) -> Commands.join(args) 4.4 rewrote them even though I have my own join function
[X] update->queue_redraw
[X] font.set_spacing, FontFile.SPACING_SPACE -> font.set_extra_spacing
[X] OS.get_system_time_msecs()
[X] Button.Align, Label.ALIGNMENT_CENTER
[X] Missing GDScriptFunctionState (commented out)
[X] PoolStringArray.join -> "".join(PackedStringArray)
[X] Invalid argument for "get_signal_connection_list()" function: argument 1 should be "StringName" but is "Dictionary". (gd4 complains if you change type of an object)
[X] Fade.gd control_all() missing arguments (this was actually a bug in my code!)
[X] Tweens - skipping add_child for now
[X] get_system_time_msecs()
[X] stubbed out some font related stuff
[X] Engine.target_fps messin up main.gd
[X] File, Dir -> FileAccess, DirAccess
[X] set_process warning (was just warning about shadowing a function, which I use on my own and I turned off the warning)
[X] resave all scenes
[X] String.plus_file -> String.path_join /String.is_rel_path
[X] array .remove -> remove_at
[X] Missing NameSpaces - looks like "namespace" is a keyword now
[X] Where I convert NodePath to Node (Look at how I fix in ScriptDebugger)
[X] handle _init in ClickMesh
[X] can't import WrightScript
